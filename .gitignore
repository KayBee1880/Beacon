# Personal learning notes — walkthroughs, decision log, diagrams.
# Not part of the product repo; kept local only.
/private/

# Android/Gradle — generated output and machine-specific IDE files.
# None of this should ever be committed: it's either regenerated by every
# build or specific to one developer's machine (SDK path, device manager state).
build/
.gradle/
.idea/
local.properties
*.iml
captures/
.externalNativeBuild/
.cxx/
